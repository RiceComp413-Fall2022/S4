import { extendTheme } from "@chakra-ui/react";

const styles = {
  global: {
    body: { color: "#fff" },
    a: { color: "#fff" },
    p: { color: "#fff" },
    h1: { color: "#fff" },
    h2: { color: "#fff" },
    h3: { color: "#fff" },
    h4: { color: "#fff" },
    Heading: { color: "#fff" },
    Text: { color: "#fff" },
    Button: { color: "#fff" },
  },
};

const Theme = extendTheme({ styles });

export default Theme;
